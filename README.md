# my-synth-shell-config

Instructions to set this configuration in your terminal:
```sh
cd ~/.config
git clone https://github.com/ch33kaboo/my-synth-shell-config.git
source synth-shell/synth-shell-prompt.sh
```
